<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <ul>
        <% users.forEach(user => { %>
            <li>
                nom: <%= user.name %> => poids: <%= user.poids %> 
            </li>
        <% }) %>
    </ul>
  </body>
</html>